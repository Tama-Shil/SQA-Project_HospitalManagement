# collectReport/apps.py
from django.apps import AppConfig

class CollectreportConfig(AppConfig):
    """
    AppConfig for the 'collectReport' app.

    This class provides configuration settings for the 'collectReport' app.
    """

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'collectReport'
